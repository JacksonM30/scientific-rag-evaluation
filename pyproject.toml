[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rag-experiment"
version = "0.1.0"
description = "CS6493 RAG experiment harness"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
